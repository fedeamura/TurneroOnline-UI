const styles = theme => ({
  root: {
    display: "flex",
    marginBottom: theme.spacing.unit * 1
  },
  separadorIcono: {
    marginRight: theme.spacing.unit
  },
  contenedorTextos: {}
});

export default styles;
